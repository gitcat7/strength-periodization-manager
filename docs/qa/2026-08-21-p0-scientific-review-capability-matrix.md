# P0 科学复盘与计划日护栏能力矩阵

基线：生产 `fac92d72e2fad0712234797a8dd6dda41738cdda`；对账分支：`1ab4325..7864419`。

| 能力 | 生产基线 | 历史分叉链路 | 本批处理 |
| --- | --- | --- | --- |
| 今日完成训练 | 浏览器分别保存组、更新状态、插入建议，失败可能留下半完成状态 | 有原子完成 RPC | 恢复为单一原子 RPC，并保留现有 Today 交互 |
| Coach 建议 | `fitness-coach.ts` 有组完成/RPE基础规则；浏览器插入，无法处理训练间隔/恢复字段 | 原子 SQL 仅覆盖旧规则 | 新增保守、可解释的规则优先级及 source revision 幂等 |
| 应用建议 | 浏览器多次更新未来动作和建议状态，未预览影响 | 同 | 新增服务端预览/确认应用，明确影响训练日，保留审计 |
| 计划日动作编辑 | 生产基线无 UI/RPC | 有 `revise_workout_prescription`、动作卡编辑 | 选择性恢复 RPC/编辑器行为，并补结构化方向和科学护栏 |
| Today/History/Progress/Coach 数据链路 | 已按 `plan_workout_exercises`、日志和建议读取 | 编辑链路已有回归测试 | 编辑保存只更新处方事实来源并增加 revision/audit；复跑全部链路回归 |
| 首页/历史日期选择 | 已在生产本次基线可用 | 旧链路部分不同 | 不覆盖、不回退 |

已知 P1，不在本批实现：跨账号客户端缓存、关键排程查询 fail-open、Progress 早期记录窗口、计划画像保存非原子、默认 release gate 未含认证 QA。
